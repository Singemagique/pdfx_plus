node_modules
dist
out
*.log*
.DS_Store
*.tsbuildinfo
.smoke

# Test coverage output (Vitest / V8).
coverage

# This is a Yarn (classic) project; never commit an npm lockfile.
package-lock.json

# Generated native build tree (node-gyp output; rebuilt by `yarn build:native` on postinstall).
# Keeping it untracked avoids committing compiled binaries and leaking local absolute paths.
native/build/

# Secrets / credentials — never commit these.
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
*.mobileprovision
test-document-to-sign.pdf
